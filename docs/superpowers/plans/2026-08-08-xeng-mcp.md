# xeng-mcp Implementation Plan

> Synced from the Cursor plan. Work in `/Users/hinha/Projects/hinha/xeng-mcp`. Twin: memoo-mcp. Upstream: x-engine.

**Goal:** Ship an npx-installable TypeScript MCP that exposes `xeng_search` and `xeng_health` against existing x-engine consumer HTTP APIs, plus an English `/x-social` skill.

**Architecture:** Thin MCP gateway; intelligence in `/x-social` skill; consumer API key only.

## Tasks

1. Scaffold package + tooling twin of memoo-mcp
2. Config + xengine client (search/health) + mocked fetch tests
3. Register only `xeng_search` and `xeng_health`
4. CLI stdio/HTTP, version/help/update
5. Author `skills/x-social/SKILL.md`
6. CI/release, README, superpowers docs

## Verification

```bash
make install && make check && make build
```

## Out of scope

Keywords tool, CMS routes, new x-engine analytics endpoints.
